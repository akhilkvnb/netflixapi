import 'package:flutter/material.dart';

class ScreenComkingsoon extends StatelessWidget {
  const ScreenComkingsoon({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.black,
    );
  }
}
